# db/database.py


def initialize_db():
    """初始化 SQLite 資料庫，建立食材表與食譜表。

    (這裡之後由同學 A，也就是你，來實作具體的 SQLite 語法)
    """
    # 暫時先用 pass 讓程式不會報錯，並生成或連線到 fridge.db
    print("[DB] 執行 database.py 中的 initialize_db()...")
    pass